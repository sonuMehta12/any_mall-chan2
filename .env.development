# Used by Vite during `npm run dev` only.
# Production build (npm run build) does NOT read this file.
#
# ── To switch servers, uncomment ONE line and comment the other, then restart npm run dev ──

# Local backend (uvicorn running on your machine):
VITE_API_URL=http://127.0.0.1:8000

# Staging server:
# VITE_API_URL=https://anymall-chan.stagingapp.in
